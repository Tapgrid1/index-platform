# Addendum to .env.example — Supabase connection strings
#
# These two lines REPLACE the localhost DATABASE_URL and DIRECT_URL
# entries at the top of .env.example when running against the Supabase
# project provisioned on 2026-08-28 (ref: grasaapfimbkevfuldlk).
#
# Copy ONLY the two lines below into your local .env (or paste the
# values via your password manager). Never commit the filled-in
# values — the existing .gitignore already covers .env.
#
# === Cut below this line into .env local ===
#
# Transaction pooler — every request uses this. The `?pgbouncer=true`
# tells Prisma to stop using prepared statements the Supabase pooler
# cannot handle. `connection_limit=1` matches one Postgrest connection
# per Vercel function instance under serverless traversal.
DATABASE_URL="postgresql://postgres.PROJECT_REF:<PASSWORD>@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
# Direct connection — prisma db push and prisma migrate only.
DIRECT_URL="postgresql://postgres:<PASSWORD>@db.grasaapfimbkevfuldlk.supabase.co:5432/postgres"
#
# === Cut above this line into .env local ===
#
# Optional (only if you wire any supabase-js client code in the future):
# - NEXT_PUBLIC_SUPABASE_URL="https://grasaapfimbkevfuldlk.supabase.co"
# - NEXT_PUBLIC_SUPABASE_ANON_KEY="<sb_publishable_…>"
# These two are safe to expose to the browser. The legacy `anon` key is
# a JWT; the 2026-refresh `sb_publishable_…` is the new equivalent.
#
# Server-only (NEVER set NEXT_PUBLIC_* on these):
# - SUPABASE_SECRET_KEY="<sb_secret_…>"  (a.k.a. service_role in legacy keys)
